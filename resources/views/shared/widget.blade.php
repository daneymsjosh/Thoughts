<div class="card">
    <div class="card-body">
        <p class="fs-4"> <span class="me-2 {{ $icon ?? 'fas fa-users' }}"></span>{{ $title ?? '' }}</p>
        <h2 class="fw-bold fs-2">{{ $data ?? 0 }}</h2>
    </div>
</div>
